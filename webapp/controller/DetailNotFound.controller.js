sap.ui.define(
  [
    './BaseController', // prettier 방지용 주석
  ],
  (
    BaseController // prettier 방지용 주석
  ) => {
    'use strict';

    return BaseController.extend('sap.ui.yesco.controller.DetailNotFound', {});
  }
);
