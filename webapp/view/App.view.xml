<mvc:View controllerName="sap.ui.yesco.controller.App"
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc" displayBlock="true">
	<OverflowToolbar id="appMenuToolbar" asyncMode="true" busy="{appModel>/isMenuBusy}" busyIndicatorDelay="0" class="app-menu-toolbar">
		<ImageContent id="logoImage" src="{ path: 'sessionModel>/Werks', formatter: '.getLogoPath' }" description="{sessionModel>/Pbtxt} eHR Home" press=".navToHome">
			<layoutData>
				<OverflowToolbarLayoutData priority="NeverOverflow"/>
			</layoutData>
		</ImageContent>

		<ToolbarSpacer />

		<!-- App Menu Position -->

		<ToolbarSpacer />

		<Button icon="sap-icon://bell" type="Transparent" />

		<Button icon="sap-icon://action-settings" type="Transparent" visible="{appModel>/isAtHome}">
			<layoutData>
				<OverflowToolbarLayoutData priority="NeverOverflow" />
			</layoutData>
		</Button>
	</OverflowToolbar>

	<App id="app" class="app-body" autoFocus="false" busy="{appModel>/isAppBusy}" busyIndicatorDelay="0" />
</mvc:View>