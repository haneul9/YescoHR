<mvc:View
	controllerName="sap.ui.yesco.controller.App"
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns:control="sap.ui.yesco.control"
	displayBlock="true"
>
<OverflowToolbar id="homeMenu" asyncMode="true" busy="{appView>/busy}" busyIndicatorDelay="{appView>/delay}">
		<ImageContent
			class="logo"
			src="image/logo_yesco.gif"
			description="yesco eHR Home"
			press=".onHomePress"
		>
			<layoutData>
				<OverflowToolbarLayoutData priority="NeverOverflow"/>
			</layoutData>
		</ImageContent>
		<ToolbarSpacer/>
		<!-- Home Menu Position -->
		<ToolbarSpacer/>
		<Button icon="sap-icon://group-2" type="Transparent"/>
		<Button icon="sap-icon://action-settings" type="Transparent">
			<layoutData>
				<OverflowToolbarLayoutData priority="NeverOverflow"/>
			</layoutData>
		</Button>
	</OverflowToolbar>
	<App
		id="app"
		autoFocus="false"
		busy="{appView>/busy}"
		busyIndicatorDelay="{appView>/delay}"
		class="app-body"
	/>
</mvc:View>