<core:FragmentDefinition xmlns="sap.m"
    xmlns:core="sap.ui.core">
    <HBox>
        <IconTabBar selectedKey="{/employee/tab/selectedKey}" expandable="false" items="{/employee/tab/list}" select=".onToggleTab">
            <layoutData>
                <FlexItemData growFactor="1"/>
            </layoutData>
            <items>
                <IconTabFilter text="{Menu1}" key="{Menuc1}"/>
            </items>
        </IconTabBar>
    </HBox>
</core:FragmentDefinition>