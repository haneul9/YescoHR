<core:FragmentDefinition xmlns="sap.m"
  xmlns:f="sap.f"
  xmlns:core="sap.ui.core">
  <VBox class="portlet portlet-p01" tooltip="{tooltip}" busy="{busy}" busyIndicatorDelay="0">
    <customData>
      <core:CustomData key="portlet-id" value="{id}" writeToDom="true" />
    </customData>
    <layoutData>
      <f:GridContainerItemLayoutData rows="{height}" columns="{width}" />
    </layoutData>
    <!-- <HBox class="portlet-header" visible="{= !${hideTitle} || ${switchable} }">
      <Title level="H2" text="{title}" visible="{= !${hideTitle} }" />
      <Text text="">
        <layoutData>
          <FlexItemData growFactor="1" />
        </layoutData>
      </Text>
      <Button icon="sap-icon://decline" type="Transparent" visible="{switchable}" class="icon-button portlet-close" press=".onPressClose" />
      <Button icon="sap-icon://add" type="Transparent" visible="{hasLink}" class="icon-button" press=".onPressLink" />
    </HBox> -->
    <HBox class="portlet-body">
      <VBox width="45%" class="my-info">
        <Image src="{sessionModel>/Photo}" mode="Background" backgroundPosition="center top" class="member-photo" width="120px" height="120px" />
        <Text text="{Ename}" class="name"/>
        <Text text="{Zzjiktlt}" class="position"/>
        <Text text="{Orgtx}" class="team" />
        <Text text="{Stltx}" class="org" />
      </VBox>
      <VBox width="55%" class="my-info-summary">
        <Label text="{i18n>LABEL_01102}" />
        <Text text="{Zyymm1}" />
        <Text text="{Period1}" class="date"/>
        <Label text="{i18n>LABEL_01103}" />
        <Text text="{Zyymm2}" />
        <Text text="{Period2}" class="date"/>
        <Label text="{i18n>LABEL_01104}" />
        <Text text="{Zyymm3}" />
        <Text text="{Period3}" class="date"/>
      </VBox>
    </HBox>
  </VBox>
</core:FragmentDefinition>