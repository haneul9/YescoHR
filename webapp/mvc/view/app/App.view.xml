<mvc:View controllerName="sap.ui.yesco.mvc.controller.app.App"
  xmlns="sap.m"
  xmlns:mvc="sap.ui.core.mvc" displayBlock="true">
  <OverflowToolbar id="appMenuToolbar" asyncMode="true" busy="{appModel>/isMenuBusy}" busyIndicatorDelay="0" class="app-menu-toolbar" height="60px">
    <ImageContent id="logoImage" src="{ path: 'sessionModel>/Werks', formatter: '.getLogoPath' }" description="{sessionModel>/Pbtxt} eHR Home" press=".navToHome">
      <layoutData>
        <OverflowToolbarLayoutData priority="NeverOverflow"/>
      </layoutData>
    </ImageContent>

    <ToolbarSpacer />

    <!-- App Menu Position -->

    <ToolbarSpacer />

    <GenericTile frameType="Auto" width="auto" class="my-info" press=".navToProfile" tooltip="{sessionModel>/Pernr}">
      <TileContent>
        <HBox alignItems="Center" height="100%">
          <Text text="{sessionModel>/Ename}" class="name" />
          <Text text="{sessionModel>/Zzjikgbt} / {sessionModel>/Zzjikcht}" />
          <Image src="{sessionModel>/Photo}" mode="Background" class="member-photo" width="26px" />
        </HBox>
      </TileContent>
    </GenericTile>

    <Button type="Transparent" class="icon-button notice" press="notifyOpenPopover" tooltip="{i18n>LABEL_01002}" />

    <Button type="Transparent" visible="{appModel>/isAtHome}" class="icon-button setting" press=".onPressPortletsP13nDialogOpen" tooltip="{i18n>LABEL_01101}">
      <layoutData>
        <OverflowToolbarLayoutData priority="NeverOverflow" />
      </layoutData>
    </Button>
  </OverflowToolbar>

  <App id="app" class="app-body" autoFocus="false" busy="{appModel>/isAppBusy}" busyIndicatorDelay="0" />
</mvc:View>