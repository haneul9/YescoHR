sap.ui.define(
  [
    // prettier 방지용 주석
    'sap/ui/yesco/mvc/controller/home/Portlets.controller',
  ],
  (
    // prettier 방지용 주석
    Portlets
  ) => {
    'use strict';

    return Portlets.extend('sap.ui.yesco.mvc.controller.home.mobile.Portlets', {});
  }
);
